<%= form_with(model: terrin, local: true) do |form| %>
  <% if terrin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(terrin.errors.count, "error") %> prohibited this terrin from being saved:</h2>

      <ul>
      <% terrin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :terrid %>
    <%= form.number_field :terrid, id: :terrin_terrid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
